{
    "enable_set_avatar": {
        "description": "启用「修改QQ头像」工具",
        "type": "bool",
        "hint": "关闭后 LLM 无法调用头像工具",
        "default": false
    },
    "enable_set_nickname": {
        "description": "启用「修改QQ昵称」工具",
        "type": "bool",
        "hint": "关闭后 LLM 无法调用昵称修改工具",
        "default": false
    },
    "enable_set_longnick": {
        "description": "启用「修改QQ个性签名」工具",
        "type": "bool",
        "hint": "关闭后 LLM 无法调用个性签名修改工具",
        "default": true
    },
    "enable_set_status": {
        "description": "启用「修改QQ在线状态」工具",
        "type": "bool",
        "hint": "关闭后 LLM 无法调用在线状态修改工具",
        "default": true
    },
    "inject_profile_prompt": {
        "description": "是否向LLM注入QQ资料工具说明提示词",
        "type": "bool",
        "hint": "开启后，LLM会知道自己可以按对话情绪自主调用昵称、签名、状态相关工具",
        "default": true
    },
    "profile_prompt": {
        "description": "QQ资料工具提示词",
        "type": "text",
        "hint": "在此修改注入给LLM的QQ资料工具提示词。可使用 {supported_statuses} 占位符自动插入当前支持的状态名称列表",
        "default": "你可以按当前对话中体现出的氛围，自主决定是否调用QQ资料工具修改自己的昵称、签名或在线状态。\n昵称和签名应简短、自然、安全，不要包含违规、攻击性、误导性的内容。\n在线状态只能从以下名称中选择：{supported_statuses}。\n头像工具不会通过提示词引导你主动调用；只有用户直接要求时才应使用更换头像相关命令。"
    }
}
